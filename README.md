# smart-car-parking-system
Smart Parking System using Arduino: An Arduino-based smart parking system that uses IR sensors to detect vehicle entry and exit, a servo motor to automate the gate, and a 16×2 I2C LCD to display available parking slots in real time. The system prevents entry when parking is full, providing a simple and efficient parking management solution.
